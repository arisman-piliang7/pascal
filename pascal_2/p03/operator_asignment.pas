//Pascal TIDAK MEMILIKI operator assignment singkat.

//Anda harus selalu menulis ekspresi lengkapnya.

Python,Pascal (Ekuivalen)
a += b,A := A + B;
a -= b,A := A - B;
a *= b,A := A * B;
a /= b,A := A / B;